############### 
# TMUX BINDINGS:
############### 

# enable <leader> r to reload tmux
unbind r
bind r source-file ~/.tmux.conf

#enabling quick tmux binding for linux MBA
unbind C-b;
set-option -g prefix `;
#bind ` send-prefix
#-- secondary prefix binding
set-option -g prefix2 C-b;

